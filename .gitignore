# Original masters live locally only — the site references the optimized copies
# in assets/. Leading slash anchors these to the repo root so assets/video/*.mp4
# and assets/img/* stay tracked.
/*.mp4
/*.mov
/*.jpg
/*.jpeg
/*.png

# local tooling
/.claude/
node_modules/
_chrome/

# Superseded hero cuts kept locally for comparison. 23MB of them, and git
# never forgets a binary - once committed they are in the history forever.
/assets/video/Old/
